import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { OnPendingScreen } from '@/components'

const MatchLocationScreen = () => {
  return (
    <OnPendingScreen/>
  )
}

export default MatchLocationScreen

const styles = StyleSheet.create({})