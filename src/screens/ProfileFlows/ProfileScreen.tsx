import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { Header } from '@/components'

const ProfileScreen = () => {
  return (
   <>
   <Header headerTitle='Cá nhân'/>
   </>
  )
}

export default ProfileScreen

const styles = StyleSheet.create({})